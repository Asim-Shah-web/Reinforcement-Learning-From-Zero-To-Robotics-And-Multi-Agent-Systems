# Reinforcement Learning Textbook — Code Companion

## The Project is in progress

Code companion for a reinforcement learning textbook, containing from-scratch implementations, experiments, and shared environments used throughout the book.

The repository is organized by chapter, with reusable environments in `code/envs/` and chapter-specific implementations in `code/chXX_*/`.

The textbook itself is maintained separately in Overleaf.
