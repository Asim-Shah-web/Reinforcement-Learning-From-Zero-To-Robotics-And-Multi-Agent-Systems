# Code Companion

This folder contains the from-scratch reinforcement learning implementations
that accompany the textbook.

Each algorithm chapter has its own subfolder. The shared `envs/` package
contains recurring environments and environment wrappers used across
multiple chapters.